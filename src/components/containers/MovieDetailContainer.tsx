import * as React from "react";
import {Movie} from "../../models";

class MoviesContainer extends React.Component<{ movie: Movie }> {
    render(): React.ReactElement {
        return (
                <div>MOVIE</div>
        );
    }
}